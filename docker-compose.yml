version: '3'

services: 
    app-golang:
        build: 
            context: ./
        container_name: app-golang
        volumes: 
            .:/go/src
networks:  
    app-golang:
        driver: bridge        